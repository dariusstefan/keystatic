---
title: "Migration from 3.3.x to 3.4.0"
description: "This section is meant to provide useful help in migrating your OpenSIPS installations from the 3.3.0 version to 3.4.0."
---

This section is meant to provide useful help in migrating your OpenSIPS installations from the **3.3.0** version to **3.4.0**.

You can find the all the new additions in the **3.4.0** release compiled [under this page](/about/version-3-4-0). The ChangeLog may help your understanding of the migration / update process.

@@red|Make sure you don't carry a left-over **Makefile.conf** file in your OpenSIPS source tree if migrating to a new branch! This configuration file, generated by *make menuconfig*, is usually not compatible with the one of a previous major release!@@

---

## DB migration

You can migrate your 3.3.x MySQL DB to the 3.4.x format using the **opensips-cli** tool :
```bash

   # opensips-cli -x database migrate 3.3_to_3.4 opensips_3_3 opensips_3_4

```
where :
* opensips_3_3 is the existing DB name corresponding to version 3.3.x format
* opensips_3_4 is the DB name to be created for 3.4.x format

See [the opensips-cli documentation](https://github.com/OpenSIPS/opensips-cli/blob/master/docs/modules/database.md#database-migration-mysql-only) for more details.

> **Observation:** 
> * the old database will not be deleted, altered or changed - it will not be touched at all
> * new database will be created and data from old DB will be imported into it

---

## Script migration

* no changes to *opensips.cfg* syntax

### Global Parameters

* [log_facility](https://www.opensips.org/Documentation/Script-CoreParameters-3-4#log_facility) has been deprecated and replaced with [syslog_facility](https://www.opensips.org/Documentation/Script-CoreParameters-3-4#syslog_facility).
* [log_name](https://www.opensips.org/Documentation/Script-CoreParameters-3-4#log_name) has been deprecated and replaced with [syslog_name](https://www.opensips.org/Documentation/Script-CoreParameters-3-4#syslog_name).
* [log_stderror](https://www.opensips.org/Documentation/Script-CoreParameters-3-4#log_stderror) has been deprecated and is now equivalent with setting the new  [stderror_enabled](https://www.opensips.org/Documentation/Script-CoreParameters-3-4#stderror_enabled) and [syslog_enabled](https://www.opensips.org/Documentation/Script-CoreParameters-3-4#syslog_enabled) parameters as following:
  * *log_stderror=yes* - *stderror_enabled=yes* and *syslog_enabled=no* (default)
  * *log_stderror=no* - *stderror_enabled=no* and *syslog_enabled=yes*.

### Core Variables

* no changes

### Core keywords

* no changes

### Core functions

* no changes

### Core MI commands

* the output of the [list_blacklists](https://www.opensips.org/Documentation/Interface-CoreMI-3-4#list_blacklists) rules have changed to improve readability:
  * *owner* - now a string
  * *expire* - indicates the number of seconds until the rule expires
  * *proto* - printed as string protocol, or "any"

### Statistics migration

* no changes

## Module migration

### B2B_LOGIC module
* The flags of the [b2b_bridge()](https://opensips.org/docs/modules/3.4.x/b2b_logic.html#func_b2b_bridge) function have been renamed as following:
  * **n** to **notify**
  * **f** to **rollback-failed**
  * **t[nn]** to **max_duration=[int]**.
* The flags of the [b2b_init_request()](https://opensips.org/docs/modules/3.4.x/b2b_logic.html#func_b2b_init_request) function have been renamed as following:
  * **a** to **transparent-auth**
  * **p** to **preserve-to**
  * **t[nn]** to **setup-timeout=[nn]**
* The **init_sdp_body** and **init_sdp_ctype** parameters of the [b2b_init_request()](https://opensips.org/docs/modules/3.4.x/b2b_logic.html#func_b2b_init_request) function have been dropped.
* The **use_init_sdp** module parameter has been dropped.

### CFGUTILS module
* the [ts_usec_delta()](https://opensips.org/docs/modules/3.4.x/cfgutils.html#func_ts_usec_delta) 5th parameter has changed:  it is now a **string** instead of an **integer**, allowing arbitrary-sized diffs to be performed (no longer limited to max 2147 seconds).

### DIALOG module
* The **val** parameter of [store_dlg_value()](https://opensips.org/docs/modules/3.4.x/dialog.html#func_store_dlg_value) is now a variable.
* the first parameter, **idx** of the [set_dlg_flag()](https://opensips.org/docs/modules/3.4.x/dialog.html#func_set_dlg_flag), [test_and_set_dlg_flag()](https://opensips.org/docs/modules/3.4.x/dialog.html#func_test_and_set_dlg_flag), [reset_dlg_flag()](https://opensips.org/docs/modules/3.4.x/dialog.html#func_reset_dlg_flag), [is_dlg_flag_set()](https://opensips.org/docs/modules/3.4.x/dialog.html#func_is_dlg_flag_set) functions is now a static string.
* The [`$DLG_flags`](https://opensips.org/docs/modules/3.4.x/dialog.html#pv_DLG_flags) variable now returns a list of flag names separted by space instead of a single integer value.

### MID_REGISTRAR module
* Check the module docs for the **flags** parameters of the [mid_registrar_save()](https://opensips.org/docs/modules/3.4.x/mid_registrar.html#func_mid_registrar_save) and [mid_registrar_lookup()](https://opensips.org/docs/modules/3.4.x/mid_registrar.html#func_mid_registrar_lookup) functions for the mapping between the old and new flag names.

### NATHELPER module
* Check the module docs for the **flags** parameters of the [fix_nated_sdp()](https://opensips.org/docs/modules/3.4.x/nathelper.html#func_fix_nated_sdp) and [nat_uac_test()](https://opensips.org/docs/modules/3.4.x/nathelper.html#func_nat_uac_test) functions for the mapping between the old and new flag names.

### REGISTRAR module
* Check the module docs for the **flags** parameters of the [save()](https://opensips.org/docs/modules/3.4.x/registrar.html#func_save) and [lookup()](https://opensips.org/docs/modules/3.4.x/registrar.html#func_lookup) functions for the mapping between the old and new flag names.

### TM module
* Check the module docs for the **flags** parameter of the [t_relay()](https://opensips.org/docs/modules/3.4.x/tm.html#func_t_relay) function for the mapping between the old and new flag names.

## Command line options migration

* The **-E** option has been removed as standard error logging is used by default until the config script is parsed and a different setting is encountered (i.e. logging to syslog and stderror disabled etc.).

## Packaging migration

### AUTH modules
* NEW package available in the repos: **opensips-auth-modules**, containing both the **[auth](https://opensips.org/docs/modules/3.4.x/auth.html)** and **[uac_auth](https://opensips.org/docs/modules/3.4.x/uac_auth.html)** modules, mainly due to their extra dependency on a TLS library to be present on the system (e.g. OpenSSL or WolfSSL)
